import { Request, Response } from 'express';

class CreateTweetController {
  public async create(
    request: Request,
    response: Response
  ): Promise<Response> {}
}

export default CreateTweetController;
